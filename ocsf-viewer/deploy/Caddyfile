# Additional sites live in the persistent Caddy data volume.
import /data/sites/*.caddy

{$SITE_HOST} {
  encode zstd gzip
  reverse_proxy app:8080
  header Strict-Transport-Security "max-age=31536000"
  log {
    output stdout
    format json
  }
}
