# Feature map

Where each owner-facing feature lives. A map, not documentation: one line per
feature, enough to open the right file without grepping. Read the linked plan
for the why. Keep it current — see the rule in [AGENTS.md](../AGENTS.md).

## Glossary — what the owner sees vs what the code says

Product naming drifted from code naming on purpose (API paths and Go
identifiers were kept stable). Grep for the right-hand column.

| Owner-facing term | In code / API |
|---|---|
| **workspace** (panel, sidebar entries) | `Environment`, `sandboxID`, `environments/*` routes, `chats/environments.go`, `sandbox.SandboxInfo` |
| **sandbox** (the VM itself) | sbx sandbox with `RuntimeName` `wc-…`/`ws-…`, `sandbox.RuntimeDriver` |
| **chat** | `chats.Chat`, `chats/*` routes; a chat's transcript is `Entry`/`conversation` |
| **preview** (a published port) | `Port`/`ports/*` routes, edge "binding", `sandbox/preview.go`, `chats/ports.go` |
| **spare** (pre-warmed sandbox) | `sandbox/pool.go`, config `sandboxes.warmSpares` |
| **grant** / **access** (documents, repos, network…) | `sharing/*` routes, `policy/sharing.go`, `chats/grants.go`; levels `read < write < structure` |
| **share documents** / **share repositories** | `sharing/select`, `sharing/github_select`, `DocumentSharing.tsx`, `RepositorySharing.tsx` |
| **pull request proposal** | `policy/pullrequests.go`, `sandbox/publish_plan.go`, `request_pull_request` tool |
| **approval** (agent asks, owner answers in the transcript) | `chats/{id}/approvals/{rid}` → `Engine.ResolveAs`, `Approvals.tsx` |
| **review** (a request only the app can settle: a pull request proposal, document suggestions, a document selection or creation) | `chats.Review`, `chats[].reviews` in `GET state`, `chats/reviews.go`; TUI `/review`, launcher `popupper.review` |
| **permission rule** (allow / deny / ask a tool pattern; a workspace's or a chat's) | `chats.Rule` in `chats/rules.go`; workspace rules on `State.Environments[sandboxID].Rules`, chat rules on `Chat.Rules`; routes `environments/{id}/rules`, `chats/{id}/rules` |
| **provider** (Codex / Claude) | `broker.Provider`, `agent/rpc.go` (Codex app-server), `agent/claude.go` |
| **owner** | the single capability holder; `edge/owner.go` role `admin`; `auth.mode: owner` |
| **principal** | who a sharing connection belongs to (`PrincipalID`), keyed for future multi-user |
| **policy** / **broker** / **gateway** | `warden policy` (`policysvc`), `chat/internal/policy` — the trusted control plane and inspected egress proxy |
| **runner** / **worker** | `warden runner` (`runnersvc`), `chat/internal/sandbox` — drives sbx |
| **edge** | `warden edge` (`edgesvc`), `chat/internal/edge` — sign-in, preview hosts, ingress |
| **guest image** | `deploy/guest/`, `release.GuestImage*`, config `sbx.guestImage` |
| **suggestion** (a proposed edit to a Google Doc), **draft** (what the owner approves) | `policy.DocumentProposals`, `document_proposals` table, `sharing/doc_*` routes, `DocHunk` (one change), `DocParagraph` |
| **egress mode** restricted / open | `policy/egress.go`, `sharing/egress_set`, `<state>/policy/egress.json` overrides `warden.json` |
| **network access** (a workspace's own egress mode, the owner's choice) | `Chat.Network`, `chats/network.go`, `POST chats` `network`, `environments/{id}/network`, `sharing/egress_set` with `sandboxID`, `Registry.SetSandboxEgress`, `<state>/policy/egress-overrides.json`, `web/src/network.ts`, `NetworkSelect.tsx`, `warden chat new --network`; plan `docs/workspace-egress-plan.md` |
| **Kubernetes shape** (the third install, beside Mac and OVH) | config `runtime.kind: kubernetes`, `config.RuntimeKubernetes`; chart `deploy/helm/warden`; drivers `sandbox/kube`, `policy/kube` |
| **tier** (isolation boundary of a sandbox pod: `kata` or `gvisor`) | config `kubernetes.tier`, `config.TierKata`/`TierGVisor`; chart `runtime.tier`; the RuntimeClass `kubernetes.runtimeClass` |
| **canary** (two throwaway pods proving NetworkPolicy is enforced) | `policy/kube/canary.go` (`CanaryOptions`, `runCanaries`), pods labelled `warden.monaddle.com/canary` |
| **cluster facts** (what the policy service reads back about the cluster before trusting it) | `policy/kube/clusterfacts.go`, `policy/kube/inspector.go` |
| **trust bundle** (system CAs plus the gateway CA, mounted into every sandbox) | `policy/kube/trust.go` (`TrustPublisher`, `TrustBundleKey`), ConfigMap `warden-guest-trust` (`kubernetes.trustConfigMap`) |
| **shared gateway** (one credentialed egress listener for every sandbox) | `policy/sharedgateway.go` (`SharedGateway`), Service `warden-gateway`, config `kubernetes.gatewayService`/`gatewayPort`, `config.GatewayShared` |
| **startup stage** (what the chat's status line says while its sandbox starts) | `sandbox.Progress` / `Stage*` (`sandbox/progress.go`), `chats.Startup` (`chats/startup.go`), `Chat.startup`, labels in `web/src/stages.ts` |
| **Cluster** (the admin console section: nodes, sandbox pods, service pods, logs) | `sandbox.ClusterInspector` / `ClusterStatus` / `PodInfo` (`sandbox/cluster.go`), `sandbox/kube/cluster.go`, `chats/cluster.go`, `ClusterView.tsx` |
| **size** (a workspace's CPUs and memory) | `sandbox.Resources` (`cpuMilli`, `memoryMB`), `sandbox.ResourceLimits` (default, max, `cpuStepMilli`, `restart`), runner op `resize` |
| **fork** (a sibling chat copied from another up to a message, its agent session copied too; optionally on a copy of the workspace) | `chats/fork.go` (`Fork`, `copyWorkspace`), `Chat.ForkSession`, `Chat.Origin`, `Request.ForkSession`/`Source`, runner op `clone` (`sandbox/clone.go`), `Entry.Fork` (role `fork`, `Workspace`/`Into`), the CLI's `--fork-session` |
| **side question** (`/btw`: answered from a copy of the session, never sent to it) | `chats/aside.go` (`Aside`), runner op `aside` (`sandbox/aside.go`), `Entry.Aside` (role `aside`) |
| **output style** (Claude's `outputStyle` setting, a launch flag) | `chats/style.go` (`SetOutputStyle`), `Chat.OutputStyle`, `BrokerConfig.OutputStyle`, `sandbox.OutputStyles` |

## Processes and layout

One binary, `chat/cmd/warden` (module `warden/chat`). `warden start` runs the
four services as subcommands of itself; `warden install|doctor|login|open|chat
|stop|status|uninstall|bugs` are the owner CLI.

| Service | Command | Package | Owns |
|---|---|---|---|
| policy | `warden policy` | `chat/internal/services/policysvc`, `chat/internal/policy` | per-sandbox policy engines, gateway CA + inspected proxy, credentials, sharing/grants, PR proposals, audit, image store |
| runner | `warden runner` | `chat/internal/services/runnersvc`, `chat/internal/sandbox` | sbx lifecycle, spares, agent streams, previews, repository fetch/review, host stats |
| chat | `warden serve` | `chat/internal/services/chatsvc`, `chat/internal/chats`, `chat/web` | chat state + HTTP API + web UI |
| edge | `warden edge` | `chat/internal/services/edgesvc`, `chat/internal/edge` | authentication (owner cookie / Google), preview hostnames, public ingress |

Shared: `chat/internal/config` (the one `warden.json` schema; appendix of
[warden-local-deployments-plan.md](warden-local-deployments-plan.md) and
appendix A of [warden-kubernetes-plan.md](warden-kubernetes-plan.md)),
`chat/internal/release` (pinned runtime versions, guest image, OAuth client
IDs), `chat/internal/handshake` (version check between services),
`chat/internal/services` (what the four share), `chat/internal/transport`
(`unix://` and mutual-TLS `tls://` listeners and dials between them),
`chat/internal/kube` (the minimal Kubernetes API client: in-cluster or
kubeconfig, typed REST, watch, exec, logs).

State dir (`~/.warden` locally): `policy/ runner/ app/ edge/ provider/ sbx/
runtimes/ bin/` (`chat/cmd/warden/state.go`).

Kubernetes shape (`runtime.kind: kubernetes`): the same four services as
Deployments over `tls://`, state under `/var/lib/warden` on one PVC each,
sandboxes as pods in `kubernetes.namespace` with a workspace PVC; installed
by the chart `deploy/helm/warden`, never by `warden install`
([warden-kubernetes.md](warden-kubernetes.md)).

Legacy macOS-VM stack (pre-SBX, still in tree): `warden` (Python launcher),
`host/` (control plane + dashboard), `proxy/` (Linux inspection VM), `native/`
(Swift launcher), `tests/`, `scripts/*.py`. Not where new chat/SBX work goes.

## Features

| Feature (owner-facing) | Backend | API | Web / TUI | Tests | Plan |
|---|---|---|---|---|---|
| Chats: create, message (retry / copy as markdown from the transcript), typing indicator, rename/archive/export (markdown or JSON, built in the browser; a subagent's entries nested under its card, the person's `!` commands attributed to them), search (⌘K palette across chats, ⌘F find in the transcript; both reach a subagent's nested entries and `!` cards, the find bar opening the card or unfolding the output that hides a match; `GET chats/search` is the same search server-side, for the TUI), jump-to-bottom and unread divider, turn timing and tokens/cost per agent turn, slash commands, `@path` and shared-resource mentions in the composer (`@doc:`/`@repo:`/`@preview:`, kept in the transcript, expanded to what the agent acts on when the message is sent), prompt history (Up/Down at the draft's edges, Ctrl-R search; from the transcript's own user entries), long pastes collapsed to `[Pasted text #N — M lines]` chips and sent in full, stop; every keyboard shortcut in one table (`shortcuts.ts`: the handlers match through `isKey`, the `?` overlay — `?` outside an input, or "Keyboard shortcuts" in the chat menu — lists it by area with the platform's modifier names; Shift-Tab cycles a Claude chat's permission mode) | `chats/engine.go`, `chats/store.go`, `chats/paths.go`, `chats/mentions.go` (`Resources`, `ExpandMentions`), `chats/search.go` (`Engine.Search`, `SearchHit`), `sandbox/paths.go` (worker op `paths`), `conversation/model.go` (`Turn`, `Usage`), `agent/claude.go` (usage from the `result`) | `POST chats`, `chats/{id}/message|typing|edit|stop`, `GET chats/{id}/file|image-file|paths|resources`, `GET chats/search?q=&limit=`, `GET state`, `GET events` (SSE) | `ChatShell.tsx`, `Conversation.tsx`, `Suggest.tsx` (+ `composer.ts`, `composer.test.ts`), `HistorySearch.tsx` (+ `history.ts`, `history.test.ts`), `Pastes.tsx` (+ `paste.ts`, `paste.test.ts`), `EntryView.tsx`, `RichText.tsx` (+ `links.ts`, `links.test.ts`), `CodeBlock.tsx` (+ `code.ts`, `code.test.ts`), `Mermaid.tsx` (+ `mermaid.ts`, `mermaid.test.ts`), `DiffView.tsx` (+ `diff.ts`, `diff.test.ts`), `ToolCard.tsx` (+ `tools.ts`, `tools.test.ts`), `katex.ts` (+ `math.ts`, `math.test.ts`), `streaming.ts` (+ `streaming.test.ts`), `InlineImage.tsx`, `Lightbox.tsx` (+ `images.ts`, `images.test.ts`), `ExportDialog.tsx`, `useCopy.ts` (+ `export.ts`, `export.test.ts`), `SearchPalette.tsx`, `FindBar.tsx` (`revealHits`; + `search.ts` `entryHits`, `search.test.ts`), `transcript.ts` (+ `transcript.test.ts`), `TurnStats.tsx` (+ `turns.ts`, `turns.test.ts`), `shortcuts.ts` (+ `shortcuts.test.ts`), `ShortcutsDialog.tsx`; TUI `tui/search.go` (`/search`), `tui/export.go` (`exportTree`) | `chats/*_test.go` (`search_test.go`), `sandbox/paths_test.go`, `conversation/turns_test.go`, `tui/tui_test.go` | [warden-chat-migration-plan](warden-chat-migration-plan.md), [chat-rich-rendering-plan](chat-rich-rendering-plan.md) |
| Stop: interrupts the agent's turn through its protocol (`turn/interrupt`; Claude Code's `interrupt` control request), the chat back as `interrupted` with the session resident and the sandbox up; an idle session is released; only a run without a live agent, or one that ignores the interrupt, is cancelled (sandbox stopped). Stopping the workspace ends the sessions too | `chats/engine.go` (`Stop`, `turn`, `endSession`), `chats/environments.go` (`stopChats`), `agent/claude.go` (`turn/interrupt`) | `chats/{id}/stop` | `Conversation.tsx` (Stop button), `composer.ts` (`/stop`); TUI `/stop` | `chats/engine_test.go`, `chats/resident_test.go`, `agent/claude_test.go` | [stop-interrupts-turn-plan](stop-interrupts-turn-plan.md) |
| Message queue: a message sent while the agent's turn runs waits in the transcript as a queued card (Warden's queue, never the CLI's) and goes as its own turn after it, several in order, attachments kept; Edit (the pencil, ↑ in an empty composer for the last queued one, TUI `/edit N`) edits it in place on its card — its slot and ID kept, Enter saves, Esc leaves it, a message the agent got meanwhile refuses the save and the draft moves to the composer — Withdraw drops it; Stop holds the queue (the interrupted chat keeps its queued messages until Send on a card, `/queue send`, or the next message; a `!` command or `#` note runs beside the held queue and leaves it held, and says so); a conversation rewind withdraws it. `warden chat send --wait` follows the message's own turn only (`--wait-all` the chat until idle). Edit-and-resend on a message the agent got: the composer holds it with a bar saying that sending rewinds the conversation to before it ("also rewind the code" for `both`), then sends the edit as a new message by the editor; Esc-Esc with an empty draft opens the rewind chooser on the last message and a conversation rewind from the chooser prefills the composer with the message | `chats/queue.go` (`Withdraw`, `EditQueued`, `SendQueued`, `withdrawQueued`), `chats/engine.go` (`Stop` holds, `confirm` moves a sent queued entry to the end), `chats/rewind.go` (`RewindResult.Withdrawn`), `tui/follow.go` (`Follow` with a message ID, `followMessage`), `cmd/warden/chat.go` (`--wait`, `--wait-all`) | `POST chats/{id}/withdraw` `{id}` → the entry, `POST chats/{id}/queued/{entryID}/edit` `{text, attachments?}` → the entry (409 once sent), `POST chats/{id}/send-queued`; `chats/{id}/rewind` then `chats/{id}/message` for an edit | `queue.ts` (+ `queue.test.ts`: `queuedMessages`, `queuedLast`, `queueHeld`, `lastQueued`, `canEditAndResend`, `heldHint`, `Editing`), `EntryView.tsx` (`QueuedActions`, `QueuedEditor`), `Conversation.tsx` (`editQueued`, `saveQueued`, `overtaken`, `withdraw`, `editing`, `prefill`), `RewindDialog.tsx` (`onClose` with the target), `stages.ts` (the count in the status); TUI `tui/queue.go` (`/queue [send]`, `/withdraw N`, `/edit [N] [both]`, `takeQueued` in place, `heldNote`, Esc Esc, ↑), `tui/render.go` (`queueMarker`, queued last) | `chats/queue_test.go`, `chats/queue_edit_test.go`, `tui/tui_test.go` (`TestQueueMarkersWithdrawAndEditLastQueued`, `TestEditCommandRewindsAndPrefills`, `TestFollowOneMessage*`), `web/src/queue.test.ts`, `web/src/stages.test.ts` | [claude-parity](claude-parity.md) item 10, § Round 2 D |
| Composer attachments (paste, drop, picker; sent into the sandbox with the message) | `chats/attachments.go`, `sandbox/attachment.go` (worker op `attachment-write`), `agent/claude.go` (image blocks) | `POST chats/{id}/attachments` (multipart), `GET chats/{id}/attachments/{aid}`, `POST chats/{id}/attachments/{aid}/remove`, `attachments` on `chats/{id}/message` | `Attachments.tsx`, `Conversation.tsx` (+ `attachments.ts`, `attachments.test.ts`, `drafts.ts`, `drafts.test.ts`) | `chats/attachments_test.go`, `sandbox/attachment_test.go` | [chat-rich-rendering-plan](chat-rich-rendering-plan.md) |
| Composer `!cmd`: a shell command run in the workspace by the person, not the agent (bash as the sandbox user, cwd the workspace, 60 s, output tail 30k; while a turn runs too), recorded as a command card attributed to them (`Sender`), transcript-only, with "Send to agent" quoting it into the draft, found by the searches and exported as "Command by …" whether or not tool steps are; `#note`: appended as a bullet to the workspace's `CLAUDE.md` with a system line and the hint that the agent reads it only once the workspace's settings are loaded ([claude-parity](claude-parity.md) item 7) | `chats/composer.go` (`Exec`, `AppendMemory`), `sandbox/exec.go` (ops `exec`, `memory-append`; `execScript`, `memoryScript`, `MemoryBullet`) | `POST chats/{id}/exec` → `{id, exitCode, timedOut, output}`, `POST chats/{id}/memory`; runner `exec` (`Request.Command` → `Response.Exec`), `memory-append` (`Request.Bytes`) | `composer.ts` (`prefixed`, `quoteCommand`), `Conversation.tsx`, `EntryView.tsx` (`by-person` card, "Send to agent"), `ToolCard.tsx` (`tool-by`), `transcript.ts` (a person's step never groups); TUI `tui/app.go` (`submit`, `shell`), `tui/render.go` (`senderLabel`) | `sandbox/exec_test.go`, `chats/composer_test.go`, `tui/tui_test.go` (`TestBangAndHashPrefixes`), `web/src/composer.test.ts`, `web/src/transcript.test.ts` | [claude-parity](claude-parity.md) item 12 |
| Provider + model choice (provider fixed per chat; every chat has a concrete model — the provider's default, Opus / GPT-5.6 Sol or config `providers.<p>.defaultModel`, `chats/defaults.go`, `agentOptions.defaults`; a Claude chat's model changes on its live session, `set_model`, with a transcript marker and the session's resolved model shown; Codex relaunches; the 1M-context variants when allowed) | `sandbox/agent_selection.go`, `chats/engine.go` `ConfigureAgentAndRelease`, `chats/settings.go` (`pushModel`, `modelRefused`), `agent/claude.go` (`model/set`) | `chats/{id}/agent`; `GET state` → `chats[].session.model`, `agentOptions.longContext`, `agentOptions.models` | `ComposerMenu.tsx` (model · effort menu with an Advanced section), `models.ts`, `models.ts`, `composer.ts`; TUI `/model` (menu from the catalog, `tui/complete.go`), `/provider`, `tui/status.go` | `chats/settings_test.go`, `agent/claude_test.go` | [model-dropdown-plan](model-dropdown-plan.md), [warden-claude-provider-plan](warden-claude-provider-plan.md), [claude-parity](claude-parity.md) § item 9 |
| Thinking budget, effort level and fast mode of a Claude chat (set any time; pushed to the live session and before every turn; fast mode and the 1M models only where config allows) | `chats/settings.go` (`SetSettings`, `applySession`, `Efforts`, `ParseThinking`), `chats/store.go` (`Chat.Thinking`, `Effort`, `Fast`, `Session.FastMode`), `agent/claude.go` (`thinking/set`, `effort/set`, `fastMode/set` → `set_max_thinking_tokens`, `apply_flag_settings`); config `providers.claude.allowFastMode`, `allowLongContext`, `providers.<p>.defaultModel` (`config.Claude`); chart `providers.claude.{allowFastMode,allowLongContext}` | `chats/{id}/settings` `{thinking, effort, fast}`; `GET state` → `chats[].thinking|effort|fast`, `agentOptions` | `ComposerMenu.tsx` (under Advanced; effort in the menu, high until chosen) (selects + Fast checkbox), `composer.ts` (`/thinking`, `/effort`, `THINKING`, `EFFORTS`); TUI `/thinking`, `/effort`, `/fast`, `tui/status.go` | `chats/settings_test.go`, `agent/claude_test.go`, `config/config_test.go`, `web/src/composer.test.ts`, `tui/tui_test.go` | [claude-parity](claude-parity.md) § item 9 |
| Permission mode of a Claude chat (auto / ask / plan, set any time, a running turn included; the CLI's mode follows through `set_permission_mode`, its own plan-mode entry comes back as a marker) | `chats/permissions.go` (`SetMode`, `decide`, `RuleFor`), `chats/store.go` (`Chat.Mode`), `agent/claude.go` (`permissions/set`, `permissions/modeChanged`) | `chats/{id}/mode`; `GET state` → `chats[].mode` | `Conversation.tsx` (Shift+Tab and `/mode`; a "Plan mode" / "Ask mode" mark under the composer, no selector), `composer.ts` (`/mode`, `MODES`); TUI `tui/app.go` (Shift-Tab, `/mode`), `tui/complete.go` (`/mode` in the menu), `tui/status.go` `StatusLine` | `chats/permissions_test.go`, `agent/claude_test.go`, `web/src/composer.test.ts`, `tui/tui_test.go` | [claude-parity](claude-parity.md) § items 3 and 4 |
| Permission rules (allow / deny / ask by a tool pattern in Claude Code's syntax — `Bash(git *)`, `Edit(src/**)`, `Read`, `WebFetch(domain:x)`, `mcp__warden__*`; a workspace's apply to every chat of it, a chat's to itself; deny answers without asking in every mode, ask forces a card even in auto, allow answers in ask; "Allow always" in this chat or this workspace; the rules editor in the workspace panel; the chat's permission history, last 200) | `chats/rules.go` (`Rule`, `NewRule`, `Matches`, `decideByRule`, `EnvironmentRecord`, `PermissionEvent`, `Engine.Rules/AddRule/RemoveRule/Permissions`), `chats/store.go` (`Chat.Rules`, `Chat.Permissions`, `State.Environments`), `chats/permissions.go` (`decide`, `RuleFor`, `RuleLabel`) | `GET environments/{id}/rules`, `POST environments/{id}/rules`, `environments/{id}/rules/{rid}/remove` (same under `chats/{id}/rules`), `GET chats/{id}/permissions`, `chats/{id}/approvals/{rid}` `scope`; `GET environments` → `rules`, `GET state` → `chats[].rules` | `PermissionsSection.tsx`, `PermissionHistory.tsx`, `rules.ts`, `Approvals.tsx` (`PermissionCard` scope buttons), `permissions.ts` (`alwaysLabel`, `alwaysHint`); TUI `tui/rules.go` (`/rules`, `/permissions`, `/allow`, `A`), `tui/render.go` hint | `chats/rules_test.go`, `chats/permissions_test.go`, `web/src/rules.test.ts`, `web/src/permissions.test.ts`, `tui/tui_test.go` | [claude-parity](claude-parity.md) § Round 2 B |
| Codex agent stream (app-server over stdio) | `agent/rpc.go`, `sandbox/runtime.go` `Stream` | | | `agent/*_test.go` | [response-streaming-plan](response-streaming-plan.md) |
| Claude agent stream (SDK control protocol; each tool call as a typed item; a subagent's frames nest under its Agent call; background commands and async subagents stay running until the CLI's task notification; a turn the CLI starts by itself after a notification is a turn to Warden) | `agent/claude.go`, `agent/claude_tools.go`, `sandbox/runtime.go` `Stream`, `chats/engine.go` (`awaitMessage`, `beginAgentTurn`) | | | `agent/claude_test.go`, `chats/resident_test.go` | [warden-resident-claude-plan](warden-resident-claude-plan.md), [claude-parity](claude-parity.md) (feature inventory and priority order) |
| Tool cards: each agent tool call as a typed transcript card — a command with its output folded, a file change as a diff (Claude's Edit/Write with the CLI's numbered hunks), a read with its path, a search with its hits, a fetch, an MCP call — on the web and the TUI. A Read that returned no text carries the file: an image read shows the image inline (its bytes stored through the chat's image store, `Tool.Read.Image` the stored id), a PDF read its size and page count, a notebook read its cells | `conversation/model.go` (`Tool`, `Read`, `ReadCell`), `conversation/conversation.go` (`Upsert`), `agent/claude_tools.go` (`claudeToolItem`, `claudeRead`), `chats/images.go` (`keepReadImage`) | `GET state` / `events` → `entries[].tool.read`; `GET chats/{id}/images/{id}` | `ToolCard.tsx` (`ReadBody`, + `tools.ts` `readCount`, `tools.test.ts`), `EntryView.tsx`, `InlineImage.tsx`; TUI `tui/render.go` (`renderTool`, `readCount`) | `agent/claude_test.go` (`TestClaudeReadOfImagePDFAndNotebook`), `chats/images_test.go` (`TestReadImageIsStoredNotKept`), `tui/tui_test.go`, `web/src/tools.test.ts` | [claude-parity](claude-parity.md) (Round 2 E) |
| Subagents: a subagent's messages and tool calls nest under its Agent card (tool-call count, elapsed time, prompt, its final text as the result, and while it runs the agent's own account of its step — Claude Code's `task_progress`: "Reading hello.txt", its count, time and tokens — as `Tool.Progress`; a subagent's subagent nests the same way); background commands and background subagents as cards that stay running until the task reports, the output landing on the command's card when the agent reads it (`TaskOutput`); the agent's todo list as one checklist card updated in place (`TodoWrite`, `TaskCreate/TaskUpdate/TaskList/TaskGet`) | `conversation/model.go` (`Entry.ParentID`, `Tool.Background`, `Tool.Progress` / `Progress`), `conversation/conversation.go` (`todoList` item), `agent/claude.go` (`task_progress`), `agent/claude_tools.go` (`claudeTodoList`, `claudeBackgroundTask`, `claudeTaskOutput`, `claudeTaskProgress`) | `GET state` / `events` → `entries[].parentID`, `entries[].tool.background`, `entries[].tool.progress`, `tool.kind == "todo"` | `transcript.ts` (`nestEntries`), `ToolCard.tsx` (`TodoList`), `EntryView.tsx` (`SubagentTranscript`), `Conversation.tsx`; TUI `tui/render.go` (`nestEntries`, `renderSubagent`, `renderTodo`), Tab expands | `agent/claude_test.go`, `conversation/conversation_test.go`, `chats/resident_test.go`, `tui/tui_test.go`, `web/src/transcript.test.ts`, `web/src/tools.test.ts` | [claude-parity](claude-parity.md) |
| Slash commands of the agent's session (Claude Code's built-ins, the workspace's commands and skills) in the composer's `/` menu; `/name …` sent as text for the CLI to expand | `agent/claude.go` (`system/init` → `thread/started` `commands`/`model`/`permissionMode`/`outputStyle`), `chats/store.go` (`Chat.Commands`, `Chat.Session`, `sessionStarted`) | `GET state` / `events` → `chat.commands`, `chat.session` | `composer.ts` (`commandItems`, `agentCommandNamed`, `AGENT_HINTS`), `Conversation.tsx`, `Suggest.tsx` (groups); TUI `tui/app.go` (`chatCommands`, `providerCommands`) | `agent/claude_test.go`, `chats/engine_test.go`, `web/src/composer.test.ts`, `tui/tui_test.go` | [claude-parity](claude-parity.md) items 5 and 7 |
| Compaction and context: `/compact [instructions]` (Claude's, passed through), the CLI's own auto-compaction, a `compaction` entry in the transcript (running while it compacts, then a divider with the trigger and the token counts, the summary the agent continues from, or the failure), and how full the context is against the model's window and the CLI's auto-compaction threshold (a meter beside the model on the web, `ctx 49k/200k (25%)` in the TUI status line, amber/red on the way to the threshold) | `agent/claude.go` (`system/status` compacting, `system/compact_boundary`, the synthetic summary `user` frame, `claudeContext` from the `assistant` frames' usage, `result.modelUsage` and `get_context_usage`), `conversation/model.go` (`Compaction`, `Context`), `conversation/conversation.go` (`compaction` item), `chats/engine.go` (`thread/context/updated`) | `GET state` / `events` → `entries[].compaction`, `conversation.context` | `ContextMeter.tsx`, `EntryView.tsx` (`CompactionDivider`), `context.ts` (+ `context.test.ts`), `stages.ts`, `export.ts`; TUI `tui/status.go` (`ContextIndicator`), `tui/render.go` (`renderCompaction`, `CompactionText`), `tui/follow.go` | `agent/claude_test.go`, `conversation/conversation_test.go`, `chats/engine_test.go`, `tui/tui_test.go`, `web/src/context.test.ts` | [claude-parity](claude-parity.md) item 8 |
| Thinking feedback: the model's reasoning as a `thinking` entry (Codex's summary streamed by delta and part; Claude's blocks as reasoning items, text withheld by its CLI), shown as each app shows it, the pending-reply row while nothing streams, status "Agent is thinking" | `conversation/conversation.go` (`reasoning` item, `Delta`, `Break`, `Entry.EndedAt`), `agent/claude.go` (`thinking` blocks, `thinking_tokens`), `chats/engine.go` (`item/reasoning/*`) | `GET state` / `events` → `entries[].role == "thinking"`, `endedAt` | `Thinking.tsx` (+ `thinking.ts`, `thinking.test.ts`), `EntryView.tsx`, `Conversation.tsx`, `stages.ts`; TUI `tui/render.go` | `conversation/conversation_test.go`, `agent/claude_test.go` | [thinking-feedback-plan](thinking-feedback-plan.md) |
| Checkpoints and rewind: before every user turn the runner snapshots the workspace as a git tree under `refs/warden/checkpoints/<message id>` (the workspace's own repository, or a private git directory beside a non-repository workspace; ignored files and `.warden/` left out; an unchanged tree reuses the previous commit); a rewind to before a user message restores that snapshot (code; the workspace as it was is recorded first under the marker's ID, `Request.Before`), truncates the transcript and asks the agent's session to forget the rest through the adapter's `conversation/rewind` — Claude Code's `rewind_conversation`, keyed by the message id every user frame carries as its `uuid`; applied at once on a live idle session, after the next `--resume` otherwise, with a fresh session and the kept transcript as recap when the agent cannot — or both; a `rewind` marker entry records it (`entry.rewind`). Undo: a conversation or both rewind keeps what it removed until the next turn starts or another rewind happens (`chat.undoRewind` names the marker); Undo on the marker (TUI `/undo-rewind [code]`) puts the entries and turns back in place, cancels a pending rewind, resumes a thread the fresh fallback dropped, or starts fresh with the restored transcript as recap when the session had rewound; the workspace stays as the rewind left it unless the checkpoint recorded before a both-rewind is restored; a notice says what was done | `sandbox/checkpoint.go` (ops `checkpoint`, `checkpoints`, `restore` with `Before`, `diff`; `managedSandbox.Checkpoints`), `sandbox/managed.go` (`prepare` `newSession`), `chats/rewind.go` (`Rewind`, `UndoRewind`, `RewoundTail`, `dropRewoundTail`, `Checkpoints`, `beforeTurn`, `applyPendingRewind`, `recap`), `chats/store.go` (`Chat.DiffBase`, `Rewind`, `Recap`, `NewSession`, `RewoundTail`, `UndoRewind`), `conversation/model.go` (`Entry.Rewind`), `agent/claude.go` (`conversation/rewind`, user `uuid`) | `POST chats/{id}/rewind` `{turnID, what: code|conversation|both}`, `POST chats/{id}/undo-rewind` `{id, code}` → `UndoResult`, `GET chats/{id}/checkpoints`; runner `restore` `before` | `RewindDialog.tsx` (a user message's hover action, Esc-Esc, `/rewind`, the chat menu), `EntryView.tsx` (`rewind` marker with Undo), `Conversation.tsx` (`undo`), `rewind.ts` (+ `rewind.test.ts`: `canUndoRewind`, `undoOffersCode`, `undoHint`, `undoOutcome`); TUI `tui/rewind.go` (`/rewind`, `/rewind N code|conv|both`, `/undo-rewind [code]`, `undoHint`), `tui/render.go` (`rewind` marker) | `sandbox/checkpoint_test.go`, `chats/rewind_test.go`, `chats/queue_edit_test.go` (`TestUndoRewind*`), `agent/claude_test.go` (`TestClaudeConversationRewind`), `tui/tui_test.go`, `web/src/rewind.test.ts` | [claude-parity](claude-parity.md) (item 11, § Round 2 D) |
| Session diff: the workspace against the chat's first checkpoint (or the one its last code rewind restored), git's unified diff per file with counts, cut at 2 MiB | `sandbox/checkpoint.go` (op `diff`, `WorkspaceChanges`), `chats/rewind.go` (`Diff`) | `GET chats/{id}/diff` | `SessionDiff.tsx` (the workspace panel's Changes › View…, the chat menu's Changes…, `/diff`) over `DiffView.tsx`; TUI `tui/rewind.go` (`/diff`, Tab expands, `RenderChanges`) | `sandbox/checkpoint_test.go`, `chats/rewind_test.go`, `tui/tui_test.go`, `web/src/rewind.test.ts` | [claude-parity](claude-parity.md) (item 11) |
| Fork a chat: a sibling chat on the same workspace whose transcript is the source's up to a chosen message (the whole of it by default; entry IDs, turns, rules, mode, style and attachments copied) with a `fork` marker naming the source; a Claude chat's session is copied at the fork's first launch (`--resume <source> --fork-session`, the runner recording nothing until the CLI reports the copy's own id), a cut before an earlier message becoming a pending conversation rewind on the copy; Codex, or a chat without a session, forks as a fresh session with the copied transcript as recap. `copyWorkspace` gives the fork a new workspace the runner clones from the source sandbox's disk (op `clone`: SBX template-save from the stopped source, Kubernetes a PVC clone or tar copy — the snapshot's image digest declared to the policy service so its image pin accepts the copy); grants stay with the source (policy scopes them per sandbox), both chats get a marker, the copy's panel says "Copied from …" | `chats/fork.go` (`Fork`, `copyWorkspace`, `copyEntries`, `keptTurns`, `copyAttachments`), `chats/rewind.go` (`recapWith`), `chats/engine.go` (the `prepare`/`stream` requests carry `ForkSession`), `sandbox/clone.go` (`cloneLocked`, `recordImageLocked`), `sandbox/runtime.go` (`createFrom`, `ImageDigest`), `policy/sbxinspector.go` (`allowedImage`) | `POST chats/{id}/fork` `{turnID?, copyWorkspace?}` → `{id, title, session, sandboxID, workspace}`; runner op `clone`, `GrantContext.ImageDigest` | `ForkDialog.tsx` (the chat menu's Fork…, a message's hover action, `/fork`, the "Copy the workspace" choice), `EntryView.tsx` (`fork` marker), `WorkspacePanel.tsx` ("Copied from"); TUI `tui/longtail.go` (`/fork [N\|all] [copy]`) | `chats/fork_test.go`, `sandbox/clone_test.go`, `policy/registry_test.go`, `tui/tui_test.go` | [claude-parity](claude-parity.md) items 15, Round 2 E |
| Side question (`/btw question`): asked of a copy of a Claude chat's resident session while it is idle by a one-shot CLI the runner starts beside the resident one with the run's brokered environment, no tools and `--no-session-persistence` (the guest script removes a copy the CLI wrote anyway), recorded as an `aside` entry (question, answer, cost) the agent never sees; refused during a turn ("after this turn") and for Codex; a released session is started for the question (the run has nothing to send and waits idle; the entry is `starting`, the chat shows its startup stages) unless a held queue would go with the start; "Ask in chat" / `/btw promote [N]` sends the question as the person's message with the answer quoted (`Aside.Promoted`); `/btw` alone lists them on the TUI | `chats/aside.go` (`Aside`, `awaitSession`, `PromoteAside`, `PromotedText`), `chats/engine.go` (`run`: no message → idle), `sandbox/aside.go` (op `aside`, `AsideCommand`, `asideScript`, `ParseAsideOutput`), `sandbox/managed.go` (`managedRun.broker`) | `POST chats/{id}/aside` `{text}` → `{id, text, error?, costUSD}`; `POST chats/{id}/aside/{entryID}/promote` → `{messageID, text}`; runner `aside` (`Request.Command`, `ThreadID`, `OutputStyle` → `Response.Aside`) | `composer.ts` (`sideQuestion`, `/btw`), `Conversation.tsx` (`ask`, `promote`), `EntryView.tsx` (`AsideCard`), `api.ts` (`promoteAside`), `export.ts`; TUI `tui/longtail.go` (`/btw`, `promoteAside`), `tui/render.go` (`renderAside`) | `chats/fork_test.go` (`TestAsideAnswersFromAnIdleSession`, `TestAsideStartsAReleasedSession`, `TestPromoteAside`), `sandbox/aside_test.go`, `tui/tui_test.go`, `web/src/composer.test.ts` | [claude-parity](claude-parity.md) item 15, § Round 2 G |
| `/cost`: this chat's turns, tokens by kind, cost (Claude's estimate; Codex reports none) and time in turns, summed locally from the turn records — a dismissable card on the web, a notice on the TUI, never sent to the agent | — | `GET state` → `conversation.turns` | `cost.ts` (`sessionCost`, `costRows`, `costLine`), `CostCard.tsx`, `Conversation.tsx`; TUI `tui/longtail.go` (`SessionCost`, `CostLines`, `/cost`) | `web/src/cost.test.ts`, `tui/tui_test.go` | [claude-parity](claude-parity.md) item 15 |
| Automatic titles: a chat created without a title ("New chat") is named after its first turn from the first message and the first reply by a one-shot, tool-less CLI on Haiku beside the resident Claude session (runner op `oneshot`: `--max-turns 1`, `--no-session-persistence`, a short `--system-prompt`); Codex, a run that ended with the turn, or a failed one-shot fall back to the first message's first line; a title given at creation or a rename wins and ends the naming (`chat.titled` auto / manual); no transcript entry | `chats/title.go` (`autoTitle`, `CleanTitle`, `FallbackTitle`, `TitleModel`, `TitleSystemPrompt`), `chats/engine.go` (`CreateFrom`, `Edit`, the run loop after a turn), `chats/store.go` (`Chat.Titled`), `sandbox/aside.go` (`OneShotCommand`, `oneshot`, `oneShotRun`) | `GET state` → `chats[].titled`; runner `oneshot` (`Request.Command`, `Model`, `Instructions` → `Response.Aside`) | `ChatShell.tsx` (the name field's placeholder), sidebar; TUI `/new` and `warden chat new` without a title | `chats/title_test.go`, `sandbox/aside_test.go` (`TestOneShotCommand`, `TestOneShotOpRunsThroughTheRuntime`) | [claude-parity](claude-parity.md) § Round 2 A |
| Spend: what the agent's turns and the person's side questions took — per chat as a chip beside the context meter (`chat.spend`: turns, tokens, cost, priced, and the answered side questions marked apart as `asides`/`asideCostUSD`), per workspace in the panel's Spend block (this chat and its siblings summed, archived included) and in the TUI's `/cost` (an "of it, side questions" row and the workspace line), and in the admin console's Spend section (today since the local midnight / last 7 days / all time, by provider, over every chat; owner-only at the edge); Codex reports tokens and no cost; titles are not counted | `chats/spend.go` (`Spend`, `spendOf`, `asideCounts`, `SpendReport`, `Engine.Spend`), `chats/engine.go` (`state` fills `Chat.Spend`), `edge/edge.go` (`ownerOnly` lists `api/spend`) | `GET state` → `chats[].spend`; `GET spend` → `{today, week, all: {turns, input, output, total, costUSD, priced, asides?, asideCostUSD?, chats, providers}}` | `spend.ts` (`chatSpend`, `workspaceSpend`, `spendLabel`, `spendSummary`, `spendLine`, `asidesLabel`), `cost.ts` (`sessionCost` with the entries), `SpendChip.tsx`, `CostCard.tsx`, `WorkspacePanel.tsx` (Spend), `SpendView.tsx` (in `AdminConsole.tsx`); TUI `tui/longtail.go` (`SessionCost`, `WorkspaceCost`, `WorkspaceCostLine`) | `chats/spend_test.go`, `edge/edge_test.go`, `web/src/spend.test.ts`, `web/src/cost.test.ts`, `tui/tui_test.go` (`TestCostShowsTheWorkspaceTotal`) | [claude-parity](claude-parity.md) § Round 2 A, § Round 2 G |
| Model catalog: at every Claude session start the engine asks the CLI for `list_models` (adapter command `models/list`) and keeps the rows per provider (value, resolved model, label, description, effort levels, adaptive thinking, fast mode) as `agentOptions.models`; the web picker and the TUI `/model` menu build their rows from it (the static rows without one), each model's effort levels come from its row (a model without any, like Haiku, takes none), and the 1M rows and the Fast checkbox stay visible but disabled with the hint naming `providers.claude.allowLongContext` / `allowFastMode`; Codex keeps its static rows | `chats/catalog.go` (`ModelInfo`, `Catalog`, `loadCatalog`, `ParseCatalog`, `catalogRows`), `chats/store.go` (`State.Catalog`), `agent/claude.go` (`models/list` → `list_models`) | `GET state` → `agentOptions.models[provider][]` | `models.ts` (`modelOptions`, `effortOptions`, `fastModeFor`, `LONG_CONTEXT_HINT`, `FAST_MODE_HINT`), `ComposerMenu.tsx`, `composer.ts` (`ModelOption.disabled`); TUI `tui/complete.go` (`ModelRows`, `modelItems`, `EffortsFor`, `effortItems`, `LongContextHint`), `tui/client.go` (`AgentOptions`, `ModelInfo`) | `chats/catalog_test.go`, `agent/claude_test.go` (`TestClaudeModelsList`), `web/src/models.test.ts`, `tui/tui_test.go` (`TestModelAndEffortMenusFromTheCatalog`) | [claude-parity](claude-parity.md) § Round 2 A |
| Notifications: on the web, while the tab is hidden, a browser Notification (opened with the chat menu's "Desktop notifications" toggle, the choice in `localStorage` `warden-notify`) on a turn's end, an approval or a failure in any chat, deep-linking to it, and a favicon badge counting them until the tab is seen; on the TUI the terminal bell on the same events and on a new review (`/bell on\|off`, kept in `<state>/tui/bell`) and the terminal title `Warden · <chat> · running\|idle\|approval` (OSC 0, pushed at start and popped at exit; a pending review counts as approval) | — | `GET events` | `notify.ts` (`notifyEvents`, `shouldNotify`), `favicon.ts` (`setFaviconBadge`), `Notifications.tsx` (`useNotifications`), `ChatShell.tsx`; TUI `tui/longtail.go` (`BellEvents`, `TitleFor`, `bellForEvents`, `setTitle`), `tui/app.go` | `web/src/notify.test.ts`, `tui/tui_test.go` (`TestTitleAndBellEvents`, `TestReviewCardAndCommand`) | [claude-parity](claude-parity.md) item 15 |
| Output style of a Claude chat (default, Explanatory, Learning): a launch setting (`--settings {"outputStyle":…}`; the pinned CLI has no flag or control request for it), so a change releases the idle session and the next message starts the process with it; the selector beside the model says when the running session differs (`chat.session.outputStyle`) | `chats/style.go` (`SetOutputStyle`, `OutputStyles`), `sandbox/aside.go` (`ValidOutputStyle`, `OutputStyles`), `sandbox/runtime.go` (`claudeSessionArgs`) | `POST chats/{id}/style` `{style}`; `GET state` → `chats[].outputStyle`; runner `stream` `outputStyle` | `ComposerMenu.tsx` (under Advanced), `composer.ts` (`STYLES`, `/style`); TUI `tui/longtail.go` (`/style`) | `chats/fork_test.go` (`TestOutputStyleAppliesAtTheNextLaunch`), `sandbox/aside_test.go` (`TestValidOutputStyle`), `web/src/composer.test.ts`, `tui/tui_test.go` | [claude-parity](claude-parity.md) item 15 |
| Approvals in the transcript (agent questions; a Claude tool ask in ask mode as its command or diff with Allow / Allow always in this chat or this workspace / Deny with a message the model reads; a plan in plan mode with approve into auto or ask, or keep planning with feedback) | `chats/engine.go` `Answer` (`ResolveAs`), `request` (`item/tool/requestPermission`), `chats/permissions.go` (`decide`, `permissionEntry`), `agent/claude.go` (`can_use_tool`, `updatedPermissions`) | `chats/{id}/approvals/{rid}` (`allow`, `always`, `scope`, `message`, `mode`, `answers`) | `Approvals.tsx` (`PermissionCard`, + `permissions.ts`, `permissions.test.ts`); TUI `tui/render.go` `RenderApprovals` (reviews, which are not approvals, sit above them: `RenderReviews`), `tui/app.go` `answerPermission`, `tui/watch.go` | `chats/permissions_test.go`, `chats/engine_test.go`, `agent/claude_test.go`, `tui/tui_test.go` | [claude-parity](claude-parity.md) § items 3 and 4 |
| Standing instructions per person (what a user-level `CLAUDE.md` would hold): kept by principal in the chat store, appended after Warden's own prompt at every launch for the chat's creator and every sender so far (Claude's `--append-system-prompt`, `--system-prompt-snapshot off`; Codex's `developerInstructions`); a queued message from a late joiner, or after a change, relaunches the session with the current blocks | `chats/instructions.go` (`Instructions`, `sessionInstructions`, `instructionsOwed`), `chats/store.go` (`State.Instructions`, `Chat.Creator`), `chats/engine.go` (`CreateFrom`, `run`, `awaitMessage`), `sandbox/runtime.go` (`RunSpec.Instructions`, `claudeSystemPrompt`, `WardenSystemPrompt`, `ChatRenderingPrompt` — what the chat renders, in both providers' prompts), `sandbox/managed.go` `launchLocked` | `GET`/`POST me/instructions`; runner `stream` → `instructions` | `InstructionsDialog.tsx` (sidebar "Instructions"); TUI `/instructions [edit|clear]` | `chats/instructions_test.go`, `sandbox/memory_test.go` (`TestClaudeSystemPromptCarriesInstructions`), `tui/tui_test.go` | [claude-parity](claude-parity.md) item 13 |
| Memory view: the workspace's `CLAUDE.md`, `CLAUDE.local.md`, `AGENTS.md`, `.claude/CLAUDE.md`, `.claude/rules/**.md` and the CLI's auto-memory directory (`memory_paths.auto` from `system/init`) with contents, one file written back at a time, a notice naming who edited what | `sandbox/memory.go` (ops `memory-list`, `memory-write`, `ValidateMemoryPath`, the guest scripts), `chats/memory.go` (`Memory`, `WriteMemory`, `MemoryView`), `chats/store.go` `Session.AutoMemory`, `agent/claude.go` `claudeThread` (`autoMemory`) | `GET chats/{id}/memory`, `POST chats/{id}/memory/write` (`scope`, `path`, `text`) | `MemorySection.tsx` (workspace panel "Memory"), `MemoryDialog.tsx`, `memory.ts` (+ `memory.test.ts`); TUI `/memory [N|FILE]`, `/memory edit N|FILE` | `sandbox/memory_test.go`, `chats/instructions_test.go` (`TestMemoryRoutes…`), `web/src/memory.test.ts`, `tui/tui_test.go` | [claude-parity](claude-parity.md) item 13 |
| Workspace panel: status, Stop / Start (a stopped sandbox back without a message; runner op `start`) / Archive (Stop and Archive end a running chat first) / Delete, sibling chats, spend, documents, repositories, memory, permission rules, PRs, previews, access history | `chats/environments.go` (`StartEnvironment`, `stopChats`), `sandbox/managed.go` `startLocked` | `GET environments`, `environments/{id}/stop|start|archive|delete`, `sharing/history` | `WorkspacePanel.tsx` (+ `MemorySection.tsx`, `PermissionsSection.tsx`) | `chats/engine_test.go`, `chats/sharing_test.go` | [warden-environments-plan](warden-environments-plan.md) § Workspace panel |
| Startup stages and live activity: the chat says which stage its start is at (queued with the blocker, binding, preparing, waiting, creating / resuming with the driver's detail, attesting, probing, installing, cloning, launching, initializing, connecting, sending, firstResponse — the last two on every turn); a start over 5 s logs its stages' durations; once the turn runs, the status line, the sidebar dot's title and the TUI status say what the agent is doing from the newest running entry ("Running go test ./...", "Editing engine.go", "Reading claude.go", "Searching for …", "Fetching example.com", "Explore agent: 3 tool calls · Reading hello.txt", "Agent is thinking", "Compacting context", else "Agent is working"; `activity.ts` and `tui/activity.go` share one fixture); runner reads never queue behind a creation | `sandbox/progress.go` (`progress` op, `Report`/`WithProgress`), `sandbox/snapshot.go` (`status`/`usage`/`pod` while `prepare` holds the lock), `sandbox/managed.go` `prepareLocked`, `sandbox/kube/driver.go` `StartupDetail`, `chats/startup.go` (`followProgress`), `Worker.PrepareTimeout` | `GET state` / `events` → `chat.startup`; `GET environments` → `chats[].stage` | `Conversation.tsx` status line, `ChatShell.tsx` sidebar dot, `WorkspacePanel.tsx` chat list, `stages.ts` (`chatStatusLabel`), `activity.ts` (`activityLabel`, `runningLabel`); TUI `tui/status.go` `StatusLine`, `tui/activity.go` (`ActivityLabel`) | `sandbox/progress_test.go`, `sandbox/kube/cluster_test.go` (`TestStartupDetail`), `chats/startup_test.go`, `web/src/stages.test.ts`, `web/src/activity.test.ts` + `tui/activity_test.go` over `tui/testdata/activity.json` | [warden-startup-visibility-plan](warden-startup-visibility-plan.md) |
| Cluster visibility (Kubernetes): the workspace's pod in the panel; admin console Cluster section with nodes, sandbox pods, Warden service pods, live usage from metrics.k8s.io, and pod logs | `sandbox/cluster.go` (`ClusterInspector`, ops `pod`, `cluster.status`, `cluster.logs`), `sandbox/kube/cluster.go`, `kube/quantity.go`, `kube/exec.go` `LogsWith`, `chats/cluster.go`; chart `templates/rbac.yaml` (`warden-runner-view`, value `rbac.runnerClusterView`) | `GET environments` → `pod`; `GET cluster`; `GET cluster/logs?namespace=&pod=&container=&tail=&previous=` (owner-only at the edge) | `WorkspacePanel.tsx` `Pod`, `ClusterView.tsx`, `units.ts` | `sandbox/kube/cluster_test.go`, `kube/quantity_test.go`, `edge/edge_test.go`, `deploy/helm/warden/test.sh` | [warden-startup-visibility-plan](warden-startup-visibility-plan.md) |
| Sandbox lifecycle (create from template, clone a workspace from another sandbox, keep-alive, idle stop, remove): a running sandbox stops `sandboxes.stopAfterIdleMinutes` (default 30) after the last chat activity — the engine reports every turn's end to the runner (`activity` op with `At`), a message's checkpoint, a command, a preview or review count too, the idle session's release does not — unless it has a published preview; a runner restart on a driver whose guests outlive it (Kubernetes) keeps the sandboxes whose guest the driver finds running at the registered generation, running with a fresh idle window, and stops nothing itself (`Reconciler`) | `sandbox/managed.go` (`initializeManaged`, `SweepIdle`, `finishManagedRun`, op `activity`), `sandbox/runtime.go` (`RuntimeDriver`, `Reconciler`, `RegisteredRuntime`, `ImageInspector`), `sandbox/clone.go` (op `clone`: SBX `createFrom` template-save, Kubernetes PVC clone/copy, `recordImageLocked`), `sandbox/lock.go`, `chats/engine.go` (`reportActivity`, `settleTurn`) | runner protocol `sandbox/client.go` (`Request.Source`, `Request.At`); `POST chats/{id}/activity` (the chat menu's "Keep workspace running") | `ChatShell.tsx` (chat menu) | `sandbox/managed_test.go` (`TestRestartKeepsTheGuestsTheDriverFindsRunning`, `TestIdleWindowCountsFromReportedActivityNotTheStreamEnd`), `sandbox/clone_test.go`, `worker_test.go`, `chats/resident_test.go` (`TestTurnEndReportsActivityToTheRunner`) | [sbx-integration-plan](sbx-integration-plan.md), [stop-status-plan](stop-status-plan.md), [workspace-keepalive-plan](workspace-keepalive-plan.md) |
| Spare (warm) sandboxes | `sandbox/pool.go` | | | `sandbox/pool_test.go` | [warden-spare-sandbox-plan](warden-spare-sandbox-plan.md) |
| Workspace size: chosen at creation, changed by the owner, requested by the agent (`request_resources`) | `sandbox/resources.go`, `sandbox/runtime.go` `createArgs`/`Resize` (SBX regenerates), `sandbox/kube/driver.go` `Resize` (`pods/resize`, live), `sandbox/managed.go` `resizeLocked`/`resizeAdoptedLocked`, `chats/resources.go`, `runnersvc/config.go` `resourceLimits`/`kubernetesResourceLimits`; config `sandboxes.memoryMB|cpus|maxMemoryMB|maxCPUs`; chart `sandboxes.{cpus,maxCPUs,maxMemoryMB}` (quota from the ceiling), Role `pods/resize` | `POST chats` `resources`, `environments/{id}/resize` (validates, then applies in the background; `GET environments` → `resizing`), `GET state` `sandboxes`; runner `resize`, `health` `limits` | `SizeSelect.tsx` in `ChatShell.tsx` (form) and `WorkspacePanel.tsx` (Resources › Change…); `warden chat new --cpus --memory` | `sandbox/resources_test.go`, `sandbox/live_sbx_test.go`, `sandbox/kube/driver_test.go` (`TestResize*`), `sandbox/kube/live_test.go`, `chats/resources_test.go` | [warden-workspace-resources-plan](warden-workspace-resources-plan.md) |
| Workspace resources: provisioned and used CPU / memory / disk (guest-reported, 3 s cache) | `sandbox/usage.go` (`usage` op, `SandboxUsage`), `chats/environments.go` (`Environment.Usage`) | `GET environments` → `usage` | `WorkspacePanel.tsx` `UsageRows` | `sandbox/usage_test.go` | [warden-environments-plan](warden-environments-plan.md) § Workspace resources |
| Host resource stats (runner's own host) | `hoststats/`, surfaced in `sandbox.Response.Stats` / `pool.go` | `chats/{id}/runtime` (status) | | | |
| Previews: bind a port, loopback `*.localhost` or public hostnames, unpublish; on Kubernetes the runner's shared mTLS preview server (config `services.runner.previews.{listen,address}`, chart `services.runner.previewPort`) | `sandbox/preview.go`, `sandbox/ports.go`, `chats/ports.go`, `chats/preview.go`, `runnersvc/main.go`, `deploy/helm/warden/templates/{services,networkpolicies}.yaml` | `GET ports`, `ports/{id}/revoke`, `ports/{id}/proxy/*`; edge `/auth/preview`; runner `https://warden-runner:7446/{publicationID}/*` | `Previews.tsx`, `WorkspacePanel.tsx` | `sandbox/preview_shared_test.go`, `chats/ports_test.go` | [warden-public-previews-plan](warden-public-previews-plan.md), [warden-kubernetes-plan](warden-kubernetes-plan.md) § decisions 5, 10 |
| Agent tools: `preview_attach`, `sandbox_bind_port`, `attach_image` | `chats/preview.go`, `chats/images.go` | MCP server `warden` in `sandbox/runtime.go` / `agent/claude.go` | | | |
| Agent-requestable grants: `request_network_access`, `request_repository_access`, `github_write`, `request_host_directory`, `sync_host_directory` | `chats/grants.go`, `policy/sharing.go` (`network_allow`, `github_write`) | `sharing/request`, `sharing/resolve`, `sharing/revoke` | transcript cards in `Conversation.tsx` | `chats/grants_test.go` | alpha.10 notes in [warden-local-deployments-plan](warden-local-deployments-plan.md) |
| Google Docs / Sheets sharing: picker, grant levels read/write/structure (Docs are read-only at every level; write/structure are Sheets levels), expiry, create-document requests (read-level grant, filled through suggestions) | `policy/sharing.go`, `policy/documents.go`, `policy/adapters.go` (`AccessRank`), `policy/gateway.go` (guest document API) | `sharing/google/*`, `sharing/files`, `sharing/select`, `sharing/state`; tools `request_google_docs_access`, `request_google_document_creation` | `DocumentSharing.tsx`; a pending request is a review on the chat (`chats/reviews.go`, `chats[].reviews`) | `policy/*_test.go`, `chats/reviews_test.go` | [google-docs-integration](google-docs-integration.md), [doc-write-workflow-plan](doc-write-workflow-plan.md), [document-api-plan](document-api-plan.md) |
| Google Docs suggestions: agent reads numbered paragraphs, proposes edits with reasons, owner accepts/rejects/edits the draft, Warden writes on approval, returned drafts revised by the agent; while pending (or stale, applying), a review on the chat (`chats[].reviews`) | `policy/docmodel.go`, `policy/docdiff.go`, `policy/docchanges.go`, `policy/docview.go`, `policy/doccompile.go`, `policy/docproposals.go`, `policy/sharing.go` (`Document`, `BatchUpdate`), `chats/reviews.go` | `sharing/doc_state`, `sharing/doc_preview`, `sharing/doc_draft`, `sharing/doc_decide`, `sharing/doc_resolve`, `sharing/doc_return`, `sharing/doc_rebase`; tools `read_google_document`, `propose_google_document_edit` | `DocumentReview.tsx`, `web/src/documents/suggestions/` (vendored from Panta, see `web/src/documents/PROVENANCE.md`) | `policy/docmodel_test.go`, `policy/docchanges_test.go`, `policy/docview_test.go`, `policy/docproposals_test.go` | [doc-suggestions-plan](doc-suggestions-plan.md), [doc-suggestions-view-plan](doc-suggestions-view-plan.md) |
| Docs inline images from attachments (`warden-image:<id>`, one-edit publish) — no producer since direct Docs writes were retired (2026-09-17); kept for suggestions to carry images | `policy/images.go`, `chats/images.go`, `imageguard/` | `chats/{id}/images/*`, `/published/<token>.png`, gateway action `unpublish` | `ImageAttachment.tsx` | | [image-attachments-ovh-plan](image-attachments-ovh-plan.md) |
| GitHub repository sharing: checkout, per-repo read categories (contents / issues / pull_requests), GitHub App or user token | `policy/githubapp.go`, `policy/githubuser.go`, `policy/operations.go` (REST catalog), `sandbox/repository.go`, `sandbox/fetch.go` | `sharing/github/*`, `sharing/github_repositories`, `sharing/github_select` | `RepositorySharing.tsx` | `sandbox/repository_test.go`, `repository_acceptance_test.go` | [github-repository-sharing-plan](github-repository-sharing-plan.md), [github-app-integration](github-app-integration.md) |
| Pull request proposals: reviewed single-branch push, owner Review proposal… dialog; while pending, a review on the chat (`chats[].reviews`) that the launcher opens the app for and the TUI shows | `policy/pullrequests.go`, `policy/gitreview.go`, `policy/gitprotocol.go`, `sandbox/publish_plan.go`, `sandbox/review.go`, `chats/proposal_file.go`, `chats/sharing.go` (`sharingTool`), `chats/reviews.go` | `sharing/pr_preview`, `sharing/pr_get`; tool `request_pull_request` | `PullRequestReview.tsx` | `sandbox/publish_plan_test.go`, `review_test.go`, `chats/reviews_test.go` | [pull-request-approval-plan](pull-request-approval-plan.md) |
| Inspected egress: gateway CA, per-host leaf certs, provider SSE streaming (`ProviderEndpoints` in `policy/stream.go`: OpenAI, Codex and Anthropic `/v1/messages`; other routes are buffered), destination policy | `policy/ca.go`, `policy/gateway.go`, `policy/gatewaypool.go`, `policy/stream.go`, `policy/egress.go`, `policy/registry.go` | runner→policy `bindGateway`, `configureProvider`, `egress`, `authorize` | | `policy/*_test.go` | [warden-gateway-ca-plan](warden-gateway-ca-plan.md), [response-streaming](response-streaming.md) |
| Egress mode restricted / open (runtime switch) | `policy/egress.go`, `policy.Registry.SetEgressMode` | `sharing/egress`, `sharing/egress_set` | `AdminConsole.tsx` | | |
| Admin console: egress mode, disconnect Google / GitHub, sign-in ledger, spend totals, bug reports received | `edge/owner.go`, `edge/bugreports.go`, `policy/sharing.go` `disconnect`, `chats/spend.go` | `/api/admin/*` (edge), `sharing/disconnect`, `spend` | `AdminConsole.tsx`, `SpendView.tsx`, `BugReports.tsx` | `chats/spend_test.go` | [admin-console-plan](admin-console-plan.md), [claude-parity](claude-parity.md) § Round 2 A |
| GitHub sign-in / refresh from the browser (user-token mode; owner-only device flow run by the policy service) | `policy/githublogin.go`, `policy/githubuser.go` `Save`, `login/github.go` `DeviceFlow` | `sharing/github_login_start`, `sharing/github_login_status`, `sharing/github_login_cancel` | `GitHubSignIn.tsx` (in `AdminConsole.tsx`, `RepositorySharing.tsx`) | `policy/githublogin_test.go`, `edge/edge_test.go` | [github-browser-login-plan](github-browser-login-plan.md) |
| Sign-in: owner mode (local capability cookie) and Google (server mode; sessions kept across restarts in `<edge state>/sessions.json`) | `edge/edge.go`, `edge/owner.go`, `edge/ledger.go`, `browserauth/` (`Config.SessionsFile`) | `/auth/*`, `/oauth/*` | `AuthRoot.tsx`, `GoogleLogin.tsx` | `edge/*_test.go`, `browserauth/auth_test.go` | [google-login-publishing-plan](google-login-publishing-plan.md) |
| Provider logins (Codex / Claude / Google / GitHub device flow) | `login/github.go`, `cmd/warden/login.go`, `cmd/warden/github_login.go`, `sandbox/openai.go`, `policy/oauth.go`, `policy/credentials.go` | | `warden login` | | [warden-local-deployments-plan](warden-local-deployments-plan.md) |
| Local install: the one-line installer (`sh -c "$(curl -fsSL …/main/install.sh)"`: newest release for the host, SHA256SUMS check, `~/.warden/releases/` + the `~/.warden/release` link, then `warden install`), `warden install|doctor|start|open|stop|status|uninstall|bugs`, private sbx namespace, runtimes layout, the bug-reports question, popups (`--popups none|notify|browser|auto|silent`; a review opens the app under all but silent) | `install.sh`, `cmd/warden/*.go` (`install.go`, `doctor.go`, `sbx.go`, `runtimes.go`, `notify.go` (`popupper`), `service.go`, `bugs.go`), `hostinfo/` | | | `cmd/warden/*_test.go` (`TestPopupsOpenTheAppForReviews`) | [warden-local-install](warden-local-install.md), [warden-local-deployments-plan](warden-local-deployments-plan.md) |
| Terminal client `warden chat` (TUI), `chat list|new|send --wait|approve`: `/` menu and `@path` completion, `/attach PATH…` (several local paths and globs, uploads sent with the next message; `@./file`, `@../file` or `@~/file` in the draft attaches that local file on send, the mention rewritten to its workspace path; `/attachments`, `/detach N`), `/export [md|json] [all] [FILE]` (the web's markdown and JSON), `/rename` `/archive` `/restore` `/delete`, `/instructions` and `/memory` (a file or the person's instructions edited in the composer, Enter saves), Claude Code's keys (Ctrl+C/D, Esc interrupts, Ctrl+O, Ctrl+L, Ctrl+A/E/U/K/W, Ctrl+R and Up/Down over a per-chat prompt history under `<state>/tui/history/`; every key is a row of `tui/keys.go`'s table, which `handleKey` dispatches through and `/keys` prints by area, vim mode's rows included — round 2 G), `!cmd` and `#note` (item 12), long bracketed pastes as `[Pasted text #N — M lines]` placeholders expanded on Enter with a chip listing them, `/paste [N]` printing one in full and Ctrl+P on a placeholder previewing its first lines above the status bar (R2.17), `/queue [send]` `/withdraw N` `/edit [N] [both]` with ↑ and Esc Esc (item 10; a queued message edited in place), `/undo-rewind [code]`, `send --wait` for the message's own turn and `--wait-all` for the chat (round 2 D); rendered into the terminal's normal buffer as Claude Code renders (final entries printed once into the scrollback, a live tail redrawn in place, a structural change clears and reprints; no alternate screen or mouse tracking, so the terminal scrolls, searches and selects text; `/find` prints the matching lines — showing the steps and expanding the transcript first when only a subagent's step or a folded output holds the term —, listings and `/help` print once, the todo list is a live panel) (item 16), status line with the run's elapsed time and what the agent is doing, pending approvals and reviews, the turn's tokens/cost and the context, `/fork` `/btw` `/cost` `/style` `/bell` and the terminal title (item 15), `/bug TEXT` and `/test bugreporting`, `/search TEXT` across every chat with `/search N` (or Enter on a hit) opening the chat and printing the entry, a review card above the approvals and `/review [N]` (opens the app on the chat for a pull request proposal, document suggestions or a document choice; `tui/review.go`, `tui/watch.go` `Watch`/`Watcher` for the launcher), vim mode (`/vim on\|off` kept in `<state>/tui/vim`: Esc for normal mode, `-- INSERT --`/`-- NORMAL --` in the status bar, motions `h j k l w b e 0 $ ^ gg G` with counts, `d c y` over them, `dd cc yy x X p P`, `i a I A o O`, `u`/Ctrl+R, `.`, `:w` `:q` `:wq` `:set novim`, `/TEXT` as `/find`, Enter sends; R2.16), unread marks (the last entry seen per chat in `<state>/tui/seen.json`; `• N` in `/chats` and the `/switch` menu, `N unread` in the status bar, a `── new ──` divider printed before the first unseen entry on switch with a note counting them; `G`/End on an empty draft/`/bottom` reprint the chat to its end; R2.18) | `tui/` | same HTTP API (`chats/search`, `chats/{id}/paths`, `chats/{id}/attachments`, `chats/{id}/edit`, `chats/{id}/exec`, `chats/{id}/memory`, `chats/{id}/withdraw`, `chats/{id}/queued/{entryID}/edit`, `chats/{id}/send-queued`, `chats/{id}/undo-rewind`, `chats/{id}/fork`, `chats/{id}/aside`, `chats/{id}/style`, `chats/{id}/bug`, `bug-test`, `environments/{id}/delete`) | `tui/app.go` (`frame`, `draw`, `earlyCommit`), `tui/keys.go` (`keyBindings`, `KeysText`, `extraKeys`, `vimRows`), `tui/render.go` (`RenderBlocks`, `entryFinal`), `tui/width.go`, `tui/complete.go`, `tui/export.go`, `tui/history.go`, `tui/paste.go`, `tui/attach.go`, `tui/vim.go`, `tui/unread.go`, `tui/queue.go`, `tui/status.go`, `tui/activity.go`, `tui/search.go`, `tui/editor.go`, `tui/follow.go`, `tui/longtail.go`, `tui/review.go`, `tui/watch.go` | `tui/tui_test.go`, `tui/vim_test.go`, `tui/activity_test.go`, `tui/keys_test.go` | [claude-parity](claude-parity.md) (items 6, 10, 12, 15 and 16, round 2 C, F and G) |
| Releases: version handshake, pinned runtimes, guest image, OAuth clients; artefacts = tarballs, server image, Helm chart (`oci://ghcr.io/monaddle-too/charts/warden`, version = tag without `v`) | `release/release.go`, `handshake/`; `.github/workflows/release.yml`, `.github/workflows/guest-image.yml`, `scripts/release.sh` (`--chart`, `--publish`), `scripts/package-chart.sh` | | | | `scripts/build-guest-image-in-sbx.sh`, [warden-guest-image-plan](warden-guest-image-plan.md), [warden-kubernetes-plan](warden-kubernetes-plan.md) § decision 17 |
| Bug reporting: opt-in at `warden install` ("Send bug reports to Monaddle?", `--bug-reports=yes|no`, kept on a re-run), `warden bugs status|on|off|send "text"|test|pending`, config `reporting.{enabled,url}`; every draft under `<state>/bug-reports/pending/` is redacted before it is written (tokens, e-mails, home prefix, secret shapes, the config's secrets and the owner capability) and shown on a standalone review page (loopback listener, random token path, "Send report" / "Don't send", 15 min) by the installer, the launcher (which watches the directory every 3 s and presents each new draft once) or `warden bugs`; triggers: a failed install step, a service exiting outside a stop (its log tail + the launcher's), a recovered panic in any service's HTTP handler, the engine's run loop, a runner op, the policy control loop or a gateway; `/bug text` (composer, TUI) and `/test bugreporting`; the cloud edge receives them (`POST /api/bug-reports`), stores and limits them, and the admin console lists them | server: `edge/bugreports.go` (`bugReports`, `validateBugReport`, `clientIP`), config `edge.bugReports` (`config.Edge`, `config.BugReports`), `edgesvc/config.go` `edgeConfig`, chart values `edge.bugReports.*` (`_helpers.tpl` `warden.config`), files `<edge state>/bug-reports/<date>/<id>.json`; client: `bugreport/` (`report.go` schema + gatherers, `redact.go`, `capture.go` (`Capturer`, `Recover`/`Trap`/`Handler`, `Pending`), `send.go`, `present.go` + `page.html`), `config.Reporting`, `cmd/warden/bugs.go` (`warden bugs`, `draftWatcher`, `exitDraft`), `cmd/warden/install.go` (`decideBugReports`, `reportFailure`), `cmd/warden/start.go` (`reportExit`), `chats/bugs.go` (`Bug`, `BugTest`), the `bugreport.SetDefault` calls in `services/*/main.go`, guards in `chats/engine.go`, `sandbox/managed.go`, `sandbox/preview.go`, `policy/registry.go`, `policy/gateway.go`, `policy/sharedgateway.go` | `POST /api/bug-reports` (edge, unauthenticated), `GET /api/admin/bug-reports?limit=&before=`, `GET`/`DELETE /api/admin/bug-reports/{id}` (owner-only); client: `POST chats/{id}/bug` `{text}` → `{drafted, id, notice}`, `POST bug-test` (owner-only at the edge) → the same | `BugReports.tsx` (in `AdminConsole.tsx`), `bugreports.ts`; client: `composer.ts` (`bugReport`, `isBugTest`, `/bug`, `/test`), `Conversation.tsx` (`reportBug`, `testBugReporting`, `composer-notice`); TUI `tui/longtail.go` (`/bug`, `/test bugreporting`), `tui/client.go` (`Bug`, `BugTest`) | `edge/bugreports_test.go` (posts `docs/bug-report-sample.json`), `config_test.go` `TestEdgeBugReports`, `helm_test.go`, `edgesvc/config_test.go`, `web/src/bugreports.test.ts`; client: `bugreport/bugreport_test.go`, `config/config_test.go` (`TestReportingDefaultsOptInAndURL`), `cmd/warden/bugs_test.go`, `chats/bugs_test.go`, `tui/tui_test.go` (`TestBugAndTestBugreportingCommands`), `web/src/composer.test.ts` | [bug-reporting-plan](bug-reporting-plan.md) |
| CI: release, guest image and OCSF workflows on the self-hosted Mac runner | `.github/workflows/*.yml`, `deploy/ci/lima-docker.yaml` | | | | [ci-mac-runner](ci-mac-runner.md) |
| Deployment (OVH compose, Caddy, systemd; local `deploy-local.sh`) | `deploy/chat/*`, `deploy/guest/*` | | | | `deploy/chat/README.md`, [ovh-html-demo-plan](ovh-html-demo-plan.md) |
| Kubernetes shape: Helm chart (four Deployments over mTLS, hardened sandbox namespace, admission policy, NetworkPolicies, quota, TLS bootstrap Job or cert-manager, Ingress), Lima/k3s dev loop, GKE Autopilot test cluster with public previews (Cloud DNS zone, cert-manager DNS-01 by Workload Identity, amd64 images built under emulation), operator guide | `deploy/helm/warden/` (`values.yaml`, `templates/_helpers.tpl` renders `warden.json`), `deploy/k8s/dev/{lima.yaml,values.yaml}`, `scripts/k8s-dev.sh`, `deploy/k8s/gke/{env.example,values.yaml,cluster-issuer.yaml}`, `scripts/k8s-gke.sh` (`addons` sets ingress-nginx's and cert-manager's requests) | | | `deploy/helm/warden/test.sh` (goldens in `testdata/`), `config/helm_test.go` | [warden-kubernetes](warden-kubernetes.md), [warden-kubernetes-plan](warden-kubernetes-plan.md) |
| Kubernetes config: `runtime.kind`, `kubernetes.*` (`namespace`, `tier`, `runtimeClass`, `guestImage`, `guestImageDigest`, `storageClass`, `workspaceSizeGi`, `gatewayService`, `gatewayPort`, `trustConfigMap`, `nodeSelector`, `tolerations`), `services.{policy,runner,chat}.{listen,address}` and `services.runner.previews`, `tls.{caFile,certFile,keyFile}`, `providers.<p>.secret`, `providers.claude.{allowFastMode,allowLongContext}`, `sandboxes.{cpus,maxCPUs,maxMemoryMB}` | `config/config.go` (`Kubernetes`, `Services`, `TLS`, `Claude`, `RuntimeKind`, `GatewayMode`, validation by kind) | | | `config/config_test.go`, `config/helm_test.go` | [warden-kubernetes-plan](warden-kubernetes-plan.md) appendix A |
| Service transport: `unix://` sockets or mutual TLS `tls://` (identity = certificate CN); `warden tls bootstrap` issues the deployment CA and the four certificates | `transport/transport.go`, `transport/ca.go`, `cmd/warden/tls.go` | | chart `templates/tls-bootstrap.yaml` | `transport/transport_test.go`, `cmd/warden/tls_test.go` | [warden-kubernetes-plan](warden-kubernetes-plan.md) § decision 5 |
| Kubernetes runtime driver: sandbox pods with a workspace PVC, stop/resume, fork by clone or copy, exec, reconcile by label (at a runner restart the pod of a registered running workspace at its generation is kept and adopted, the rest deleted); `warden runner --kubeconfig` for development | `sandbox/kube/driver.go` (`Reconcile`), `sandbox/kube/spec.go`, `sandbox/kube/exec.go`, `runnersvc/main.go` `runtimeDriver` | runner protocol unchanged | | `sandbox/kube/driver_test.go` (`TestReconcileKeepsResidentPodsAndRetiresTheRest`), `sandbox/kube/live_test.go` (needs a cluster) | [warden-kubernetes-plan](warden-kubernetes-plan.md) § work item 4, [workspace-keepalive-plan](workspace-keepalive-plan.md) |
| Kubernetes enforcement: inspector (cluster facts, per-pod facts, egress by label), canaries, trust-bundle publisher, Secret credential store; `warden policy --kubeconfig` / `--kube-namespace` for development | `policy/kube/inspector.go`, `policy/kube/clusterfacts.go`, `policy/kube/canary.go`, `policy/kube/refresh.go`, `policy/kube/trust.go`, `policy/kube/secretstore.go`, `policy/kube/labels.go`, `policysvc/main.go` | | chart `templates/sandbox-{namespace,networkpolicies,admission,quota}.yaml`, `templates/rbac.yaml` | `policy/kube/*_test.go`, `policy/kube/live_test.go` (needs a cluster) | [warden-kubernetes-plan](warden-kubernetes-plan.md) § work item 5, decisions 9, 11, 12 |
| Shared gateway: one egress listener for every binding, credential in the proxy URL, 407 challenge | `policy/sharedgateway.go`, `policy/gateway.go` (`BindingGateway`), `config.GatewayMode` | Service `warden-gateway`; `Proxy-Authorization: Basic bindingID:capability` | | `policy/sharedgateway_test.go` | [warden-kubernetes-plan](warden-kubernetes-plan.md) § decision 4 |
| Edge-minted owner capability (owner mode over a `tls://` upstream: the edge mints, persists and logs the launch URL) | `edge/capability.go`, `edgesvc/main.go` | `/auth/*` unchanged | | `edge/capability_test.go` | [warden-kubernetes-plan](warden-kubernetes-plan.md) § step 6 |
| Guest base image for Kubernetes sandboxes (no SBX template; pinned by platform manifest digest) | `deploy/guest/Dockerfile.base`, `deploy/guest/build-base.sh`, `.github/workflows/guest-image.yml` (`warden-guest-base`) | | chart `guestImage.{repository,digest}` | | [warden-kubernetes-plan](warden-kubernetes-plan.md) § work item 6 |
| Audit events / SIEM export | `policy/audit.go`, `policy/redact.go`, `policy/retention.go`, `schemas/audit-event.schema.json` | | | | [siem](siem.md) |
| Untrusted image normalisation | `imageguard/` | | | `imageguard/*_test.go` | |
| Kubernetes end-to-end suite: chat/preview/stop-resume flows and the adversarial networking rows against a deployed release | driven through the edge HTTP API and `kube.Client` exec | uses the edge API and `pods/exec` | `scripts/k8s-dev.sh test` | `chat/tests/k8s/*_test.go` (build tag `k8s`, needs `WARDEN_K8S_KUBECONFIG`) | [warden-kubernetes-plan](warden-kubernetes-plan.md) § work item 8, [warden-kubernetes.md](warden-kubernetes.md) § Development |

## API surface at a glance

Chat service (`chat/internal/chats/http.go`, all under `/api/`, bearer token
from the edge): `state`, `events`, `environments`, `environments/{id}/{stop,
archive,delete,start,resize}`, `me/instructions`, `chats`, `chats/search`, `chats/{id}/{agent,message,typing,edit,stop,
activity,exec,memory,memory/write,runtime,file,image-file,paths,attachments,rewind,undo-rewind,checkpoints,diff,withdraw,send-queued,fork,aside,style,bug}`, `bug-test`, `chats/{id}/attachments/{aid}[/remove]`, `chats/{id}/queued/{entryID}/edit`, `chats/{id}/aside/{entryID}/promote`,
`chats/{id}/approvals/{rid}`, `chats/{id}/images/*`,
`ports`, `ports/{id}/{revoke,proxy/*}`, `cluster`, `cluster/logs`, `spend`, `sharing/*` (forwarded to the policy
service: `status, files, select, request, get, resolve, revoke, history,
blocked, block, connect, callback, disconnect, github_list,
github_repositories, github_select, github_write, github_login_start,
github_login_status, github_login_cancel, network_allow, egress,
egress_set, pr_preview, pr_get`).

Runner protocol (`chat/internal/sandbox/client.go`): versioned request/response
over a private socket; responses may carry `Stats` (`hoststats.Sample`, the
runner's host), `Usage` (`SandboxUsage`, one guest), `Progress` (a startup
stage), `Pod`, `Cluster` or `Logs` (the cluster view), `Checkpoint` /
`Checkpoints` / `Restore` / `Changes` (the checkpoint ops). Operations:
`health, stats, bind-chat, prepare, stream, cancel, status, activity, stop,
start, remove, resize, progress, pod, cluster.status, cluster.logs, usage,
file, stat, image-file, proposal-file, paths, attachment-write, exec,
memory-append, aside, oneshot, checkpoint, checkpoints, restore, diff, host.import, host.export,
preview.attach, preview.remove, review.publish-plan, review.latest,
review.read, review.capture` (`sandbox/managed.go` `dispatch`).

Policy service internal endpoints (`chat/internal/policy/registry.go`):
`register, check, begin, end, gateway, configureProvider, bindGateway, proxy,
egress, egress.finish, authorize, active, event`.

## Docs index

Per-feature plans are `docs/*-plan.md`; the plan that is currently the
mainline's record of progress is
[warden-local-deployments-plan.md](warden-local-deployments-plan.md). The
Kubernetes shape: design and progress in
[warden-kubernetes-plan.md](warden-kubernetes-plan.md), operator guide
[warden-kubernetes.md](warden-kubernetes.md). Architecture and security scope:
[architecture.md](architecture.md). Local install: [warden-local-install.md](warden-local-install.md).
