# OVH lightweight workflow deployment

Objective: commit/push the preserved Warden features and streaming fix, deploy on OVH with two resident 1536 MiB sandboxes, and have Codex write and serve a simple Canton HTML page using the existing plan. Agent must perform page/server work itself. No CF-docs build or PR publication.

Steps: inspect production and preserve state; configure small allocations; run release checks/build; push dedicated branch; deploy with rollback; grant demo inputs and run agent; approve its preview binding; verify authenticated preview and two simultaneous small environments; preserve results and clean up.

Progress: production13cd008 idle, 7.7GiB host with ~4.2GiB available; existing resident limit1. Updated Compose to explicit1536MiB and resident limit2. Main checkout has unrelated changes, untouched. Work based on codex/warden-stall-retry in dedicated worktree.

Release94efcd6 pushed to origin/codex/warden-ovh-html-demo and deployed to /opt/warden/releases/94efcd6. All346 Python tests passed with PYTHONPATH=host; Go tests/vet and frontend build passed; new public-callback test passed separately. Private state backup /var/backups/warden/html-94efcd6; previous release retained. Added strict HTTPS callback support for chat's GoogleConnection (legacy local adapter stays loopback-only), registered https://warden.monaddle.com/oauth/google_docs/callback in existing Monaddle OAuth client, installed its existing client configuration privately on OVH, and completed fresh browser consent directly to OVH. No laptop token/session database copied.

Local 4GiB demo stopped, files/proposals preserved; launcher setting returned to1536MiB. OVH main demo chat8e374813b0ac756aec8cfa8629717896, sandbox6b34065785d4e88d2ae39540ac65f6d2, received24h read-only grant to existing Canton plan before first prompt. Independent capacity-check conversation4463276aebaaa1a284a2024620159bd1 launched concurrently. Agents perform all guest work; controller only sets permissions and inspects.

The first agent authored site/index.html and style.css, launched a detached Python server on3000, verified HTTP200 and ~1.30GiB available, and requested/received preview approval. Preview: https://77fbc3a6410bb2336e18afda417419c9.preview.monaddle.com/. The second conversation exposed global single-run admission despite resident2. Updated chat scheduling, per-chat active run/approval routing and worker admission for two independent runs (same sandbox stays exclusive). Race tests cover concurrent streams, cancellation isolation, shared-sandbox queuing, worker capacity and idempotent preparation; tests and vet passed. Next: deploy concurrency fix and verify actual simultaneous OVH runs.

Live overlap at c1b1219 revealed both streams disconnected while the runner stayed healthy (no OOM/restarts). Policy logs show BrokenPipe on control responses: the global attestation verifier serialized two sandboxes beyond the client's10s deadline. Increased control deadline to30s (still below worker60s/provider120s leases), retaining fail-closed renewal and adding failure diagnostics. Revalidating with both provisioned environments.

The30s renewal fix removed stream disconnects, but audit confirmed proxy503s from gateway RPC's separate8s deadline. Matched it to30s and increased verifier proof reuse from2s to20s (within existing30s maximum; live gateway health remains checked). Added proof-reuse/expiry test and retained fail-closed changed-policy/runtime tests. Stopped the retry runs before redeploy; no guest artifacts edited by controller.

Completed: release2c495d9 live on OVH, with backup /var/backups/warden/html-2c495d9. Real Codex tool calls overlapped: capacity check at1789491096.332 and HTML verification at1789491097.985, each sleeping45s; both finished idle without errors. Both report MemTotal1520184kB (~1.5GiB configured). Available guest memory: HTML1372676kB, capacity1385048kB. OVH available2141MiB during overlap. Agents performed every guest write/server restart/check themselves. HTML/CSS HTTP200 before/after sleep; original preview binding restored by agent. Signed-in Chrome visually verified final page; anonymous HTTP303. Capacity sandbox stopped and chat archived to free a slot; HTML preview retained.

Final checks:348 Python tests passed; all Go tests, race tests for chats/sandbox and vet passed; frontend build passed. Branch origin/codex/warden-ovh-html-demo contains preserved prior features and all fixes. This demo reused the existing Google plan with explicit24h read-only access; no new document or GitHub PR was created. Source snapshot preserved in workspace .local/warden-ovh-html-source before cleaning the dedicated worktree. No remaining required work.
